import React from 'react';

const Votes = () => {

    return (
        <div>
            <h2>Votes</h2>
            <button>up</button>
            <button>down</button>
        </div>

    )
}

export default Votes;